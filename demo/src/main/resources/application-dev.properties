spring.profiles.active=@spring.profiles.active@

spring.jackson.serialization.WRITE_DATES_AS_TIMESTAMPS=false

server.servlet.context-path=/

## MongoDB properties
spring.data.mongodb.uri=mongodb+srv://leaf-user:LDpHHxqCow7IMcsv@cluster0.9gotc.mongodb.net/leafdemo

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=200KB
# Max file size.
spring.servlet.multipart.max-file-size=20MB
# Max Request Size
spring.servlet.multipart.max-request-size=22MB


## LEAF
# Whitelisting
leaf.whitelisting.enabled=false

# First user
leaf.firstuser.enabled=true
leaf.firstuser.email=admin
leaf.firstuser.password=admin

# Hostname
leaf.protocol_hostname=http://localhost:8080/

# Emailing
leaf.email-service=sendgrid
# mailgun configs
mailgun.api.url=https://api.mailgun.net/v3/techno-sophia.com/messages
mailgun.api.from=No-Reply <postmaster@techno-sophia.com>
mailgun.api.key=<todo: add properties with global variables>

# sendgrid configs
sendgrid.api.key=${SENDGRID_API_KEY}
sendgrid.email.from=<change here>
sendgrid.templates.account-creation=<change here>
sendgrid.templates.password-change-key-sending=<change here>
